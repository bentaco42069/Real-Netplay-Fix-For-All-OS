# Claude Phone Remote — host dependencies
aiohttp>=3.9
pyautogui>=0.9.54

# Optional extras (nice to have, not required):
#   qrcode        -> prints a scannable QR in the terminal
#   pygetwindow   -> lets Windows auto-focus the Claude window
# Install them with:  pip install qrcode pygetwindow
